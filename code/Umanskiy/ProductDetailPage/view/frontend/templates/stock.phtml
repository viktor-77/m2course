<?php /** @var $block \Magento\Catalog\Block\Product\View */ ?>

<h4>
    <?php if ($block->getProduct()->getData('type_id') === 'simple') {
    if ($block->getProduct()->getData('quantity_and_stock_status')['is_in_stock']) {
        echo " is in stock";
    } else {
        echo " not in stock";
    }
}
    ?>
</h4>
